function changeColor() {
  document.querySelectorAll('.green').forEach(element=>element.className='reset')
  document.querySelectorAll('.red').forEach(element=>element.className='reset')
  document.querySelectorAll('.orange').forEach(element=>element.className='reset')
}